<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="review">

	<select id="total" resultType="Integer">
	select count(*)
	from bBoard
	where board_category = #{board_category}
	</select>
	
	<select id="list" resultType="BoardVO">
	select b.* from
	(select rownum board_no, b.* from (select * from bBoard where board_category=#{board_category} order by board_num) b
	order by board_no desc) b
	where board_no between #{beginList} and #{endList}
	</select>
	
</mapper>