<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="android">
	<select id="noticeList" resultType="BoardVO">
		select rownum no, n.*
		from (select * from bboard where board_category = 0 order by board_num desc) n
		where rownum between #{begin} and #{end}
	</select>
	
	<select id="trafficList" resultType="TrafficVO">
		select rownum, n.*
		from (select * from traffic order by tra_num desc) n
		where rownum between #{begin} and #{end}
	</select>
	
	<insert id="trafficInsert">
		insert into traffic (tra_user_email, tra_username, tra_user_imageURL, tra_content, tra_imageURL)
		values (#{tra_user_email}, #{tra_username}, #{tra_user_imageURL, jdbcType=VARCHAR}, #{tra_content}, #{tra_imageURL, jdbcType=VARCHAR})
	</insert>
</mapper>